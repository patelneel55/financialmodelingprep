const assert = require('assert');

describe('Test', () => {
    describe('#valueEquals', () => {
        it('should be 4 when 2+2 is called', () => {
            assert.equal(2+2, 4);
        })
    })
})